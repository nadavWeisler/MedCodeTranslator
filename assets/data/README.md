# Deprecated vocabulary path

Runtime terminology datasets live in **`data/vocabularies/`** (used by `db/database.ts` and CI refresh workflows).

This directory previously held duplicate demo datasets. Do not add new vocabulary JSON here — update `data/vocabularies/` instead.
